APP_ENV=local
APP_PORT=8000
TZ=Europe/Istanbul

SOCIAL_SCHEDULER_INTERVAL=60

# API keys are stored from the web panel in data/config.json.
# Keep real secrets out of git. Copy this file to .env before Docker tests.
